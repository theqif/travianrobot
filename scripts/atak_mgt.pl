
use lib '../lib';
use strict;
use Travian;

while (1)
{
  print `date`;

  # check if $current_time_epoch is > than list of pending ataks.
   # launch ataks
   # remove form pending_ataks
   # add next atak to pending
  # parse any reports & update launch schedule accordingly
 


  `sleep 30`;
}
